import { StyleSheet } from 'react-native';
import values from '../../../modules/values/mainValues';

export default style = StyleSheet.create({
    starIcon: {
        width: values.imagesMini / 1.5,
        height: values.imagesMini / 1.5,
        marginLeft: values.marginSmall,
    }
})