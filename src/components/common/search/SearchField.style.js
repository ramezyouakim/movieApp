import { StyleSheet } from 'react-native';
import colors from '../../../modules/values/colors';
import values from '../../../modules/values/mainValues';

export default style = StyleSheet.create({
    searchField: {
        backgroundColor: colors.white,
        height: 50,
        margin: values.marginMeduim,
        padding: values.paddingSmall
    }
})